import 'package:flutter/material.dart';

class AppColors {
  // Colores base
  static const primary = Color(0xFF1E3A8A); // Azul industrial
  static const secondary = Color(0xFF16A34A); // Verde éxito
  static const error = Color(0xFFDC2626); // Rojo

  // Superficies
  static const surface = Color(0xFF0F172A); // Fondo oscuro
  static const card = Color(0xFFF1F5F9); // Tarjetas

  // Texto
  static const onPrimary = Colors.white;
  static const onSecondary = Colors.white;
  static const onSurface = Colors.white;
  static const onError = Colors.white;

  // Colores extra (no estándar)
  static const warning = Color(0xFFEA580C); // Orange
  static const accent = Color(0xFF7C3AED); // Purple
}
